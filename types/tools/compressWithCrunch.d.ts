import { SinglePackOptions } from '../index';
/**
 * Compress texture with the S3TC compression format
 */
export declare const compressWithCrunch: (args: Required<SinglePackOptions<'s3tc'>>) => Promise<any>;
